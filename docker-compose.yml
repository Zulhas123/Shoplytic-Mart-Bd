services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: shoplytic
      POSTGRES_PASSWORD: shoplytic
      POSTGRES_DB: shoplytic
    ports:
      - "5432:5432"
    volumes:
      - shoplytic_db:/var/lib/postgresql/data

volumes:
  shoplytic_db:

